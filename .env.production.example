# ============================================================================
# CONFIGURAÇÕES DE PRODUÇÃO - Gateway API
# ============================================================================
# 
# IMPORTANTE: Este arquivo serve como referência para configuração de produção.
# Copie as variáveis necessárias para seu .env de produção.
#
# CHECKLIST ANTES DE IR PARA PRODUÇÃO:
# [ ] Alterar APP_ENV para 'production'
# [ ] Desativar APP_DEBUG
# [ ] Configurar APP_URL com domínio real
# [ ] Configurar banco de dados de produção
# [ ] Configurar Redis de produção
# [ ] Configurar credenciais TREEAL de produção
# [ ] Configurar FRONTEND_URL para CORS
# [ ] Gerar nova APP_KEY e JWT_SECRET
# [ ] Configurar certificado SSL
# [ ] Configurar backup de banco de dados
# [ ] Configurar monitoramento (Sentry, etc.)
#
# ============================================================================

# Configurações da Aplicação
APP_NAME="Gateway API"
APP_ENV=production
APP_KEY=base64:GERE_UMA_NOVA_CHAVE_AQUI
APP_DEBUG=false
APP_URL=https://api.seudominio.com.br
APP_TIMEZONE=America/Sao_Paulo

# Configurações de Log (produção: menos verbose)
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# Configurações do Banco de Dados (produção)
DB_CONNECTION=mysql
DB_HOST=seu-banco-producao.com
DB_PORT=3306
DB_DATABASE=gateway_api_prod
DB_USERNAME=gateway_prod
DB_PASSWORD=SENHA_FORTE_AQUI

# Configurações de Sessão
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true

# Configurações de Cache (usar Redis em produção)
CACHE_STORE=redis

# Configurações do Redis
REDIS_CLIENT=phpredis
REDIS_HOST=seu-redis-producao.com
REDIS_PASSWORD=SENHA_REDIS_AQUI
REDIS_PORT=6379

# Configurações JWT
JWT_SECRET=GERE_UM_NOVO_SECRET_AQUI_64_CARACTERES
JWT_EXPIRATION_HOURS=24

# Configurações CORS (IMPORTANTE para segurança)
FRONTEND_URL=https://app.seudominio.com.br

# ============================================================================
# CONFIGURAÇÕES TREEAL - PRODUÇÃO
# ============================================================================
#
# IMPORTANTE: Solicite à TREEAL:
# 1. Certificado digital de produção (.PFX)
# 2. Credenciais OAuth2 de produção (client_id/client_secret)
# 3. URLs de produção das APIs
# 4. Chave PIX de produção
# 5. Configuração de webhooks
#
# ============================================================================

TREEAL_ENVIRONMENT=production

# URLs de Produção (CONFIRMAR COM TREEAL)
# Remover -h e .hmg das URLs de homologação
TREEAL_QRCODES_API_URL=https://api.pix.amplea.coop.br
TREEAL_ACCOUNTS_API_URL=https://secureapi.bancodigital.onz.software/api/v2

# Certificado de Produção
# Coloque o certificado em storage/app/certificates/
TREEAL_CERTIFICATE_PATH=CERTIFICADO-PRODUCAO.pfx
TREEAL_CERTIFICATE_PASSWORD=SENHA_DO_CERTIFICADO

# Credenciais OAuth2 - Accounts API (Cash Out)
TREEAL_ACCOUNTS_CLIENT_ID=SEU_CLIENT_ID_PRODUCAO
TREEAL_ACCOUNTS_CLIENT_SECRET=SEU_CLIENT_SECRET_PRODUCAO

# Credenciais OAuth2 - QR Codes API (Cash In)
TREEAL_QRCODES_CLIENT_ID=SEU_CLIENT_ID_QRCODES_PRODUCAO
TREEAL_QRCODES_CLIENT_SECRET=SEU_CLIENT_SECRET_QRCODES_PRODUCAO

# Chave PIX de Produção
TREEAL_PIX_KEY_SECONDARY=SUA_CHAVE_PIX_PRODUCAO

# Taxas (ajustar conforme necessário)
TREEAL_TAXA_PIX_CASH_IN=0.00
TREEAL_TAXA_PIX_CASH_OUT=0.00

# Webhook Security
# Configurar na plataforma Finance da TREEAL:
# URL: https://api.seudominio.com.br/api/treeal/webhook
TREEAL_WEBHOOK_SECRET=SEU_WEBHOOK_SECRET_SE_DISPONIVEL
TREEAL_WEBHOOK_IPS=

# Retry e Timeout
TREEAL_MAX_RETRIES=3
TREEAL_RETRY_DELAY_MS=1000
TREEAL_TIMEOUT=30
TREEAL_CONNECT_TIMEOUT=10

# Ativar integração (só depois de tudo configurado!)
TREEAL_STATUS=true

# ============================================================================
# OUTRAS CONFIGURAÇÕES DE PRODUÇÃO
# ============================================================================

# Segurança - IPs permitidos para saques (se aplicável)
ALLOWED_IPS=

# Queue (usar Redis ou database em produção)
QUEUE_CONNECTION=redis

# Mail (configurar para produção)
MAIL_MAILER=smtp
MAIL_HOST=smtp.seudominio.com.br
MAIL_PORT=587
MAIL_USERNAME=noreply@seudominio.com.br
MAIL_PASSWORD=SENHA_EMAIL
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@seudominio.com.br
MAIL_FROM_NAME="${APP_NAME}"
