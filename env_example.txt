# Configurações da Aplicação
APP_NAME="PlayGame Oficial"
APP_ENV=production
APP_KEY=base64:SUA_CHAVE_AQUI
APP_DEBUG=false
APP_URL=https://playgameoficial.com.br
APP_TIMEZONE=America/Sao_Paulo

# Configurações de Rota Admin
ADM_ROUTE=admin
EFI_ENV=production

# Configurações de Idioma
APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=pt_BR

# Configurações de Manutenção
APP_MAINTENANCE_DRIVER=file

# Configurações de Performance
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# Configurações de Log
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Configurações do Banco de Dados
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=playgameoficial
DB_USERNAME=usuario_banco
DB_PASSWORD=senha_banco

# Configurações de Sessão
SESSION_DRIVER=file
SESSION_LIFETIME=60
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

# Configurações de Broadcast
BROADCAST_CONNECTION=log

# Configurações de Arquivos
FILESYSTEM_DISK=local

# Configurações de Queue
QUEUE_CONNECTION=database

# Configurações de Cache
CACHE_STORE=database

# Configurações do Memcached
MEMCACHED_HOST=127.0.0.1

# Configurações do Redis
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Configurações de Email
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Configurações da AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Configurações do Vite
VITE_APP_NAME="${APP_NAME}"

# Configurações JWT
JWT_SECRET=SUA_CHAVE_JWT_AQUI

# Configurações Pixup
PIXUP_CLIENT_ID=
PIXUP_CLIENT_SECRET=
PIXUP_BASE_URL=https://api.pixupbr.com/v2/

# Configurações BSPay
BSPAY_CLIENT_ID=
BSPAY_CLIENT_SECRET=
BSPAY_BASE_URL=https://api.bspay.com.br/

# Configurações Woovi
WOOVI_TOKEN=
WOOVI_WEBHOOK_TOKEN=

# Configurações de Segurança
ALLOWED_IPS=192.168.1.100,10.0.0.50
ENCRYPTION_KEY=SUA_CHAVE_DE_CRIPTOGRAFIA_AQUI

# Configurações de Timeout
API_TIMEOUT=30
WEBHOOK_TIMEOUT=10
