# Testes em PR: garante que a base está estável antes de merge.
# Em push para main/master, os testes rodam dentro do deploy.yml (uma vez só).
name: Tests (CI)

on:
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Run Tests
    uses: ./.github/workflows/run-tests.yml
